<%= form_for([:admin, @preference]) do |f|%>
	<%= f.select :artist_sort_order, ["ASC", "DESC"] %>
	<%= f.select :song_sort_order, ["ASC", "DESC"] %>
	<%= f.select :allow_create_artists, [true, false] %>
	<%= f.select :allow_create_songs, [true, false] %>
<% end %>